# 一晚上手搓大模型训练全流程：从小白视角看懂 Pretrain / SFT / LoRA / DPO / GRPO

> 这份文档配合你今晚在单卡 RTX 3080 Ti 上真实跑出来的 MiniMind 实验来讲。
> 不堆术语，先用大白话和类比讲懂，再落到你自己跑出来的真实数字。读完你应该能：
> ① 说清大模型训练每个阶段在干嘛、为什么需要它；② 看懂 loss 曲线和显存日志；
> ③ 面试时用**你自己的实验数据**回答问题。

---

## 0. 先建立最重要的一张「全景图」

把训练一个大模型想象成**培养一个人**，每个阶段对应人生的一个阶段：

| 阶段 | 人生类比 | 模型在学什么 | 你今晚跑了吗 |
| --- | --- | --- | --- |
| **预训练 Pretrain** | 从小到大**博览群书**、疯狂阅读 | 学会语言规律、常识、世界知识（但不会"听话答题"） | ✅ |
| **监督微调 SFT** | 请家教、上培训班，学会**按题目格式作答** | 听懂指令、用对话格式回答问题 | ✅ |
| **LoRA** | 一种**省钱的补习方式**（不重新学全部，只补关键的一点点） | 用极低成本做领域/风格适配 | ✅ |
| **DPO / GRPO** | 培养**品味和价值观**，在好答案和烂答案之间学会挑好的 | 对齐人类偏好（说得更得体、更有用） | DPO✅ / GRPO待跑 |

一条主线流程是这样的（箭头表示"在上一步的成果上继续"）：

```
原始海量文本                问答配对数据              好/坏回答偏好数据
     │                          │                          │
     ▼                          ▼                          ▼
[预训练 Pretrain]  →基座→  [监督微调 SFT]  →会聊天→  [DPO/GRPO 对齐]  →更好用
   学语言/知识               学指令遵循                  学偏好/价值观
                                  ▲
                                  │ LoRA 是 SFT（或任意阶段）的"低成本替代打法"，不是新的阶段
```

**三句话记住本质**：
1. **Pretrain 让模型"有学问"，SFT 让模型"会交流"，DPO/GRPO 让模型"有品味"。**
2. LoRA 不是一个新阶段，而是一种**少花钱办同样事的训练技巧**。
3. 越往后，数据越"精"、学习率越小（因为模型已经很强，只需轻轻调整，不能把前面学的冲垮）。

---

## 1. 预训练 Pretrain：模型到底在学什么？

### 1.1 本质：就是一个超级"完形填空"

预训练阶段，模型只做一件事：**预测下一个词（Next Token Prediction）**。

给它一句话的前半部分，让它猜下一个 token 是什么。比如：

```
输入："今天天气真"     → 模型要预测下一个词更可能是"好"，而不是"桌子"
```

它看了几百万、上亿条这样的文本，反复做"猜下一个词、对答案、改错"，慢慢就**内化了语法、事实、推理模式**。注意：这个阶段喂的是**纯文本，没有问题-答案格式**，所以它学到的是"语言本身"，不是"如何回答问题"。

### 1.2 loss 是什么？为什么从 7.7 降到 2.5 很重要？

- **loss（损失）**：衡量"模型猜的下一个词"和"真实下一个词"差多远的一个数字，**越小越好**。用的是"交叉熵"，你现在不用记公式，理解成**猜错的惩罚分**即可。
- 一个更直观的指标叫**困惑度 PPL = e^loss**，可以理解成"模型每猜一个词，平均在多少个候选里纠结"。
  - 你 medium 预训练结束 loss=2.53 → PPL≈e^2.53≈**12.6**，即平均在十几个候选里挑（小模型这个水平正常）；
  - 刚开始 loss=7.31 → PPL≈1495，等于完全瞎猜。**从 1495 个里瞎蒙 → 十几个里挑，这就是"学会了语言"。**

### 1.3 对照你的真实实验（这是你最该记住的一组数字）

| 实验 | 数据量 | 步数 | 耗时 | loss 起→终 |
| --- | --- | --- | --- | --- |
| small 预训练 | 6 万条 | 3,750 | 10.3 分钟 | 7.73 → **3.35** |
| medium 预训练 | 15 万条 | 9,376 | 25.5 分钟 | 7.31 → **2.53** |

**同样的模型、同样的超参数，只是喂的数据多了 2.5 倍，最终 loss 从 3.35 降到 2.53，生成的话也明显更通顺。**
这就是你亲手做出来的"**数据规模效应**"对照实验，面试时这是非常硬的谈资（很多候选人只会背"数据越多越好"，你能拿出自己的曲线）。

### 1.4 这一阶段涉及的几个高频名词（一次性讲清）

- **from scratch（从零训练）**：模型初始权重是随机的，啥也不会，完全靠这批数据学起来（区别于"拿别人训好的模型继续训"）。
- **epoch（轮）**：把全部数据完整看一遍叫 1 个 epoch。你设了 2，就是看两遍。
- **batch / step**：一次喂 batch 条数据、更新一次参数叫一个 step。你 batch=32。
- **梯度累积 accumulation_steps=8**：显存小、一次塞不下那么多样本时，就分 8 次算、攒够了再更新一次，**等效于 batch=32×8=256 的大 batch**（叫"有效 batch"）。这是小显存训大模型的关键技巧。
- **学习率 lr=5e-4 + cosine 调度 + warmup**：学习率是"每次改错改多大幅度"。warmup=开头先小步热身、cosine=之后平滑地由大到小收尾，这样训练更稳。
- **bf16 混合精度**：默认用 32 位小数算很占显存，bf16 用 16 位存中间结果，**显存几乎减半、速度更快，精度损失极小**。你全程用的就是它。
- **AdamW**：最常用的优化器（决定"具体怎么按梯度改参数"），知道名字即可。

### 1.5 产出

一个 `pretrain_768.pth` 权重文件（132MB），这就是你的"**基座模型**"。但关键是——**它此时还不会聊天**，下一节用你的消融实验证明。

---

## 2. 监督微调 SFT：为什么"博览群书"之后还要"上培训班"？

### 2.1 一个最有说服力的现象（你自己的消融实验）

拿只做过预训练的模型，让它"用 Python 写斐波那契函数"，它输出的是：

```
。1, 2, 3, 5, 8, 9, 7, 9, 10, 5, 7, 9 ...（一串数字）
```

它没有在"回答你"，它只是在**续写**它认为最可能出现的文本——因为预训练里它从没见过"问题→回答"这种结构。

经过 SFT 之后，同一个模型会说："这是一个使用 Python 编写的函数：```python ...```"。**它学会了自己是个"助手"，要按格式回应你。**

> 这种"只改变一个条件（做没做 SFT）、其他都不变，来看差别"的实验，叫**消融实验（ablation）**，是科研和工程里证明"某个模块到底有没有用"的标准做法。你已经无意识地用了这个方法。

### 2.2 SFT 具体做了什么？

- 训练数据从"纯文本"换成**(指令/问题, 理想回答)的配对**，而且会套上一个固定的**对话模板（chat template）**，比如 `<|im_start|>user ... <|im_end|> <|im_start|>assistant ...`，让模型学会角色边界。
- **只对"助手回答"那部分算 loss，不对用户的提问算 loss**（你不需要模型学着怎么提问，只学怎么答）。
- **从预训练权重"热启动"**：不是从零，而是站在 pretrain 的肩膀上继续。

### 2.3 对照你的数字

| 实验 | 数据量 | 步数 | 耗时 | loss 起→终 |
| --- | --- | --- | --- | --- |
| small SFT | 2 万条 | 2,500 | 8.0 分钟 | 3.91 → **3.17** |
| medium SFT | 5 万条 | 6,250 | 19.6 分钟 | 2.82 → **2.21** |

### 2.4 一个面试高频问题：为什么 SFT 的学习率(1e-5)比预训练(5e-4)小这么多？

因为预训练阶段模型是"白纸"，要大步学语言；到 SFT 时模型已经掌握语言了，只需要**轻轻调整**它的行为方式，学习率太大会把预训练学到的东西冲坏（这叫**灾难性遗忘**）。记住这个规律：**越靠后的阶段，学习率越小。**

### 2.5 Pretrain vs SFT 一张表分清

| | 预训练 Pretrain | 监督微调 SFT |
| --- | --- | --- |
| 数据 | 海量纯文本 | 问题-回答配对 |
| 目标 | 预测下一个词，学知识/语言 | 学指令遵循、对话格式 |
| 学习率 | 大（5e-4） | 小（1e-5） |
| 从哪开始 | 随机权重（从零） | 从预训练权重继续 |
| 训完能干嘛 | 会续写，但不会答题 | 能以助手身份对话 |

---
## 3. LoRA：为什么只训练 0.61% 的参数就够了？

### 3.1 先讲痛点

全量微调（full SFT）要把模型 **63.91M 个参数全部更新**，每个任务存一个 **132MB** 的新权重，训练时占 **7.4GB** 显存。
假如你要给"医疗、法律、客服、人设……"各做一个专用模型，就得存 N 份 132MB、训 N 次全量——又慢又占地方。

### 3.2 LoRA 的思路（小白版）

**冻结住原模型，一个参数都不动；只在每层旁边挂两个很小的小矩阵 A 和 B，让训练只发生在 A、B 上。**

- 数学上：原来的变换是 y = W·x，LoRA 改成 y = W·x + (B·A)·x，其中 W 冻住，只学 A、B。
- 直观比喻：原模型是一本**不能涂改的教科书**，LoRA 是给每章贴一张**薄薄的便利贴**做补充。换个科目就换一叠便利贴，教科书本身不动。
- **低秩（low-rank）**：A、B 被设计得很"窄"（这就是"秩/rank"），所以参数量极小。直觉是："适配一个新领域所需的新信息维度其实不多，不需要动用和原模型一样大的矩阵"。

### 3.3 你的真实对比（这组数字面试极好用）

| 指标 | 全量 SFT | LoRA | 差距 |
| --- | --- | --- | --- |
| 可训练参数 | 63.91 M | **0.393 M** | LoRA 只有全量的 **0.61%（1/163）** |
| 保存的权重文件 | 132 MB | **0.779 MB** | 小约 169 倍 |
| 显存峰值 | 7.36 GB | **4.60 GB** | 省 37% |
| 2 万数据训练耗时 | 479 秒（2 轮） | 256 秒（3 轮） | 轮次更多还更快 |

推理时把这张 0.78MB 的"便利贴"贴回冻结的原模型即可（你跑过 --weight full_sft --lora_weight lora_demo，验证了能正常加载、正常生成）。

### 3.4 你观察到的现象，以及为什么是"对的"

你发现 LoRA 的 loss 一直在 2.21 附近波动、没有明显下降。**这不是失败，反而说明问题：**
你是在一个已经训好的基座上、用**同样分布**的数据做 LoRA，基座本来就已经会了，所以 loss 不会再大降；而它没有升高、没有崩坏，说明 LoRA **没有破坏原模型的能力（无灾难性遗忘）**。LoRA 真正的威力要在"**专门领域小数据**"（比如医疗问答）上才体现为明显提升。这个判断你要能讲出来，说明你不是只会跑脚本。

### 3.5 什么时候用 LoRA

显存不够、要快速适配多个领域/风格/人设、希望每个任务只存一个几百 KB 的小文件并随时热插拔——这些都是 PEFT（参数高效微调）的典型场景，LoRA 是其中最主流的方法。

---

## 4. DPO：不用训练"评委模型"，也能教模型品味

### 4.1 要解决什么问题

SFT 之后模型会回答了，但同一个问题往往有很多种答法，有的更好、有的更差。怎么让模型**稳定地偏向人类更喜欢的那种**？这一步叫"**偏好对齐**"。

### 4.2 DPO 的思路：直接给它"好答案 vs 坏答案"

- 训练数据是三元组：(问题, 更好的回答 chosen, 更差的回答 rejected)，你用了 **17,166 对**。
- 训练目标用大白话说：**让好答案出现的概率往上抬，让坏答案的概率往下压，把两者的差距越拉越开。**
- 它内部需要两个模型：
  - **policy（策略模型）**：正在被训练、不断调整的那个；
  - **reference（参考模型）**：训练前冻结的一份"快照"，当**参照物/锚**，防止 policy 为了讨好偏好而跑偏太远（用 KL 散度衡量"偏了多远"并加以惩罚，KL 你理解成"两个模型输出分布的距离"即可）。

### 4.3 为什么你的 DPO loss 一开始正好是 0.693？（超爱考的细节）

DPO 的 loss 初始值理论上等于 **-ln(2) ≈ 0.693**。原因很直观：
> 训练刚开始时，policy 和 reference 完全一样，模型对 chosen 和 rejected"**五五开**"，即给好答案的概率是 0.5；而 -ln(0.5) = 0.693。

随着训练，模型越来越能区分好坏，这个 loss 就往下走。**你日志里的起点确实就在 0.69 附近——理论和你的实验对上了，这种细节在面试里非常加分。**

### 4.4 你的真实数据 & 为什么曲线"抖"、学习率为什么极小

- 区间均值：前半 **0.6415 → 后半 0.6201**，末步 0.43、最低 0.39，缓慢下降；
- **学习率只有 4e-8**（比 SFT 还小三个数量级）：偏好对齐是"微调品味"，步子一大就会把 SFT 学会的能力冲垮，所以必须极小；
- **曲线噪声很大**：因为 batch 只有 4，每次只看 4 对数据，波动天然大，所以要看**滑动平均/区间均值**的趋势，而不是纠结每一个尖峰（你的图里画了移动平均线，就是这个原因）；
- policy+reference 两个模型一起在显存里，所以显存 5.71GB，比单模型略高。

### 4.5 一个重要认知：DPO 只改"偏好/风格"，不增加知识

你可以观察到：DPO 之后回答更**简短、收敛、不啰嗦**了，但它**依然会把斐波那契函数写错**——因为 DPO 不教新知识，模型本来不会的还是不会。**对齐阶段调整的是"怎么说"，不是"知道什么"。** 这句话很能体现理解深度。

---

## 5. GRPO：让模型"自己跟自己比"来变强（你下一步要跑的）

### 5.1 有了 DPO 为什么还要 GRPO？

DPO 需要你**事先准备好**"好/坏成对"的标注数据，成本高。GRPO（Group Relative Policy Optimization，DeepSeekMath/R1 带火的方法）换了个思路：**不需要别人喂好坏对，让模型自己批量生成、自己比较。**

### 5.2 工作流程（小白版）

对同一个问题：
1. 让当前模型**一次性采样 G 个回答**（你设 G=6）；
2. 用一个**奖励规则**给这 6 个回答分别打分（奖励可以是：规则分，如长度合适、格式正确、不重复；也可以是可验证结果，如数学答案对不对；也可以是奖励模型打的分）；
3. **组内相对比较**：算这 6 个分的平均分和标准差，某个回答比组内平均好就鼓励、比平均差就抑制。公式：优势 = (自己的分 − 组内均分) / 组内标准差；
4. 同样用 KL 约束别偏离参考模型太远。

**最大特点：不需要 PPO 里那个又贵又难训的"价值网络(critic)"，用"一组样本的平均分"当基线，所以更省、更稳。**

### 5.3 为什么 GRPO 特别慢？

因为它每训练一步，都要先让模型**当场生成 6 条、每条最长 1024 token 的回答（这个过程叫 rollout）**，"逐字生成"比"看现成文本训练"慢得多。这也是为什么我建议你用小子集、调短生成长度先跑通。你准备用的是**纯规则奖励版**（不下 3.6GB 的奖励模型），规则只奖励"长度合适、有规范的思考标签、不重复"，所以它主要验证 GRPO 的**机制**（平均奖励上升、KL 受控、组内优势被拉开），而不评判内容对错——这点要心里有数。

### 5.4 三种"对齐/强化"方法横向对比

| | SFT | DPO | GRPO |
| --- | --- | --- | --- |
| 数据 | 问题→标准答案 | 问题+好答案+坏答案 | 只要问题，答案模型自己生成 |
| 是否要在线生成 | 否 | 否 | **是（慢的根源）** |
| 要不要奖励/价值模型 | 不要 | 不要 | 不要价值网络；奖励可规则/可验证/可模型 |
| 学什么 | 会答题 | 偏好成对里挑好的 | 自己采样、组内比较着进步 |

---
## 6. 看懂训练日志：把今晚的指标变成可迁移的能力

以后你看任何训练任务，主要盯这几类东西：

### 6.1 loss 曲线怎么看
- **正常**：整体震荡下行，最后趋于平稳（收敛）。
- **一直不降**：学习率太小 / 数据有问题 / 模型没接上正确权重。
- **直接爆炸成 NaN/飞上天**：学习率太大、数值不稳（可换 bf16、加梯度裁剪）。
- **剧烈抖动**：batch 太小（你的 DPO 就是例子），看滑动平均别看单点。
- **牢记：loss 低 ≠ 回答好**。loss 只反映"拟合训练文本"的程度，生成质量必须另开推理实测（你发现 loss 2.2 仍会胡说，就是这个道理）。

### 6.2 资源类指标
- **显存峰值**：决定你能不能在这张卡上跑（你全程 4.6–7.4GB，12GB 卡绰绰有余）。省显存手段：bf16、梯度累积、梯度检查点、LoRA、GQA、减小 batch/序列长度。
- **GPU 利用率**：你长期 96–99%，说明 GPU 一直在干活、没有被数据加载"饿着"，是健康状态；如果长期只有 20–30%，说明瓶颈在 CPU/IO。
- **吞吐 tokens/s、每步耗时**：估算总训练时长用。
- **温度**：长时间满载注意散热（你峰值 72–78°C，正常）。

### 6.3 两个思想方法
- **控制变量 / 消融**：想证明"数据量有用"，就只改数据量、其他全不变（你的 small vs medium）；想证明"SFT 有用"，就对比有/无 SFT（你的 pretrain-only 测试）。
- **Chinchilla 规律**：模型参数量和训练 token 数要匹配，**小模型也要喂够 token 才不会"营养不良"**。你只用了约 5% 的全量语料，所以 63M 小模型仍出错，正是这个规律的体现，也是后续"喂全量数据/换更大模型"的改进方向。

---

## 7. 把今晚的劳动变成简历和面试弹药

### 7.1 对照岗位 JD，你已经覆盖的点
- 继续预训练（Pretrain）✅、指令微调 SFT ✅、LoRA 参数高效微调 ✅、偏好对齐 DPO ✅、强化学习 GRPO（进行中）
- Transformer 架构 / GQA（KV 头压缩）✅、bf16 混合精度 ✅、梯度累积 ✅
- 数据流程：全量下载、等间隔抽样构造子集、分布覆盖 ✅
- PyTorch 原生手写 RMSNorm/RoPE/GQA/SwiGLU 并通过 12 项数值自检 ✅

### 7.2 面试可能被问 & 你可以怎么答（用自己的数据最有底气）
1. **预训练和微调区别？** → 用第 2.5 节那张表 + 你"pretrain 只会续写、SFT 才会答题"的消融现象。
2. **为什么 SFT/对齐阶段学习率要调小？** → 防止灾难性遗忘；给你的 lr 序列 5e-4→1e-5→4e-8。
3. **LoRA 原理、为什么省显存、省多少？** → W+BA、只训 A/B；你的 0.61% 参数、132MB→0.78MB、7.36→4.6GB。
4. **DPO loss 初始为什么是 0.693？** → policy=ref 时好坏五五开，-ln0.5=ln2=0.693；你的日志正好验证。
5. **DPO 和 PPO/GRPO 区别？** → DPO 直接用成对偏好、免奖励模型；GRPO 组内采样相对比较、免价值网络但要在线生成。
6. **GQA / MHA / MQA？** → Q 头数不变，K/V 头分组共享，压缩 KV Cache；头数相等是 MHA、KV 只有 1 头是 MQA。
7. **bf16 为什么省显存？梯度累积解决什么？** → 16 位存中间结果；用多次小 batch 累积等效大 batch。

### 7.3 你的"数字速记卡"（面试前扫一眼）

```
模型：63.91M（hidden768 / 8层 / GQA KV头4 / 词表6400 / bf16）
Pretrain : 6万→3.35 ; 15万→2.53（数据×2.5，loss明显更低）
SFT      : 2万→3.17 ; 5万→2.21（lr 1e-5，只对回答算loss）
LoRA     : 只训0.393M=0.61%，权重0.78MB，显存4.6GB，256秒
DPO      : 17166对，β=0.15，lr 4e-8，loss 0.693→0.62，显存5.71GB
资源     : 显存峰值4.6–7.4GB / GPU利用率96–99% / 推理47–100 tok/s
手写组件 : RMSNorm/RoPE/GQA/SwiGLU/DecoderBlock，12项CPU自检全过
```

---

## 8. 名词小词典（随时回查）

- **token**：模型处理文本的最小单位（可以是一个词、子词或字）。
- **参数(parameter)**：模型里可学习的数字，63.91M 就是 6391 万个。
- **权重 / checkpoint(.pth)**：训练好的参数存成的文件，即"模型存档"。
- **梯度 / 反向传播**：衡量"每个参数该往哪调、调多少"的机制，不用深究数学，知道是"改错的依据"即可。
- **梯度裁剪(grad clip)**：限制一次更新的最大步长，防止某步"用力过猛"训崩。
- **优化器(AdamW)**：按梯度真正去改参数的算法。
- **warmup / cosine**：学习率"先热身升上去、再平滑降下来"的调度策略。
- **fp16 / bf16**：16 位半精度数值格式，省显存、提速度；bf16 数值范围更大、更不容易训崩，优先用。
- **KV Cache**：推理时缓存已经算过的 Key/Value，避免重复计算、加快生成；GQA 就是压缩它。
- **tokenizer**：把文字和 token 数字互相转换的"词典+切词器"。
- **chat template**：规定 user/assistant/system 角色怎么拼进模型的固定格式。
- **过拟合**：把训练数据背得太死、遇到新问题反而不行。
- **灾难性遗忘**：学新东西时把旧能力冲没了（所以后段 lr 要小、LoRA 冻结主干、DPO 加 KL 约束）。
- **policy / reference / reward model**：策略=在学的模型；参考=冻结的旧版当锚；奖励模型=专门给回答打分的模型（你用规则替代了它）。
- **KL 散度**：衡量两个概率分布差多远，训练里用来"别让新模型偏离旧模型太远"。
- **rollout**：用模型实际生成回答的过程（GRPO 慢就慢在这）。

---

## 9. 建议的继续学习顺序

1. **先把这份文档和 experiments/ 里的三张曲线对照看**，把数字和概念对应上；
2. 读你自己写的 from_scratch/model.py（RMSNorm/RoPE/GQA/SwiGLU，都有中文维度注释），跑 python test_model.py 看 12 项自检；
3. 读 notes/architecture.md、notes/alignment.md 的公式推导（RoPE、DPO 闭式解、GRPO 优势）；
4. 切回**有卡模式**，把 GRPO 用纯规则奖励跑通，补上最后一块拼图；
5. 有余力再看原始论文：RoPE、GQA、LoRA、DPO、DeepSeekMath(GRPO)、FlashAttention、Chinchilla。

> 一句话总结今晚：**你在一张消费级显卡上，把工业界训练大模型的"预训练→微调→高效微调→偏好对齐"主链路亲手走了一遍，并且每一步都有自己的对照数据和客观分析——这正是大模型岗位最看重的动手与理解能力。**